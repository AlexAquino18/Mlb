# Slim deps for Vercel Python (must stay under ~500 MB total with numpy/pandas).
# pybaseball is intentionally NOT used here — it pulls scipy/matplotlib/pyarrow and exceeds the limit.
requests>=2.28.0,<3
pandas>=2.0.0,<3
numpy>=1.26.0,<3
